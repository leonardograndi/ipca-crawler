// Criando ambiente virtual em python 3
python3 -m venv venv

// Ativando ambiente
source venv/bin/activate

// Desativando ambiente
deactivate

// Instalando dependencias
pip install -r requirements.txt